# ============================================================
# UNMASKED — Python Dependencies (unmasked-agents)
# ============================================================
# Install: pip install -r requirements.txt
# Python: 3.11+ recommended (3.10 minimum for TypedDict features)
# Verified: June 2026
# ============================================================

# ─── LLM & Agent Framework ───────────────────────────────────
langgraph==1.2.4              # NOTE: 1.x has API changes from 0.x — uses new graph builder syntax
langchain==1.3.4
langchain-core==1.4.1
langchain-openai==1.2.2
langchain-community==0.4.2
openai==2.41.0

# ─── Web Framework ────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1

# ─── Task Queue ───────────────────────────────────────────────
celery==5.6.3
redis==8.0.0

# ─── Database ─────────────────────────────────────────────────
asyncpg==0.31.0               # async PostgreSQL driver (agents use this)
psycopg2-binary==2.9.12       # sync PostgreSQL driver (for Celery tasks)
sqlalchemy==2.0.50             # ORM layer if needed later

# ─── HTTP & Utilities ─────────────────────────────────────────
httpx==0.28.1                  # async HTTP client for Spring Boot callbacks
python-dotenv==1.2.2           # .env file loading
python-json-logger==3.3.0      # structured logging

# ─── Data Processing ──────────────────────────────────────────
numpy==2.4.6                   # required by pgvector operations
tiktoken==0.13.0               # token counting for RAG chunking

# ─── Web Scraping (for RAG knowledge base ingestion) ──────────
beautifulsoup4==4.14.3
requests==2.32.5
lxml==6.1.1

# ─── Testing ──────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# ─── Dev Utilities ────────────────────────────────────────────
black==26.5.1
ruff==0.15.16
